# 顺序工作流验收记录

日期：2026-09-21。本次验证针对顺序工作台，测试替身与真实模型验证分别记录。

| 检查项 | 结果 |
| --- | --- |
| 上传后进入模型提问，不平行生成文档/书签 | 通过；自动化与接口联调 |
| 每轮最多 5 问，支持回答后继续澄清 | 通过；结构校验及问答循环测试 |
| 需求确认前不生成技术文档 | 通过；后端拒绝跳步，界面禁用未来步骤 |
| 文档确认后才允许保存书签 | 通过；确认时书签数仍为 0，点击保存后为 1 |
| 保存真实 Git 快照并绑定当前文档 | 通过；读取 commit 内容、排除 .env |
| 改文档/改代码使旧确认失效 | 通过；需要重新审阅、确认和保存书签 |
| 未勾选需求、文档、开发、验收确认时不能继续 | 通过；浏览器逐项核对 |
| 开发结束停在验收；通过后只进入下一轮文档 | 通过；接口与浏览器走到第二轮，未自动开发 |
| 重复/并发操作不能多次开发 | 通过；互斥与过期 revision 测试 |
| 自动化测试 | 14/14 |
| 类型检查、Lint、生产构建 | 通过 |
| 桌面与移动端 | 桌面与 390×844 手机截图核对；手机页面宽度为 390，无横向溢出 |
| 文件与状态持久化、重启中断恢复 | 通过；磁盘读取及恢复逻辑测试，重启服务核对 |
| 真实 Codex 生成质量、真实编码任务 | 待验；此次未调用真实模型 |

## 复核步骤

1. 首页只有导入 PRD 是当前步骤，其余显示依赖顺序。
2. 上传 PRD 后模型先分析和提问；回答完成后查看需求确认单。
3. 勾选确认需求后才出现技术文档；修改文档需重新确认。
4. 确认文档进入书签步骤；保存成功后才显示开发入口。
5. 开发结束检查结果与证据，验收后进入下一轮文档，重新走确认和书签。
6. 「项目记录」按轮次回看技术文档、确认与代码快照。

真实使用第 2 步起会调用用户配置的本机 Codex。交付联调使用独立数据目录和明确标注的固定测试执行器；其输出不代表真实模型能力。

截图 `sequence-home.png` 为新版首页。`sequence-questions-test.png` 展示模型提问；`sequence-round2-test.png` 为 390px 手机视图，展示第二轮等待文档确认。两者使用明确标注的隔离测试数据。此前的 desktop/approval/mobile/workspace 图片属于旧版历史记录，不代表当前界面。
