# 项目状态

- 当前交付：Starter Studio 顺序开发工作台。
- 最近更新：2026-09-21。
- 当前流程：PRD → 模型提问/用户回答 → 确认需求 → 本轮技术文档 → 文档确认 → 代码书签 → 开发 → 验收 → 下一轮文档。
- 已实现：单一当前任务、后续步骤锁定、按轮次归档、真实模型结构化分析/规划接入、真实 Git 快照、后端步骤校验和状态持久化。
- 验证：14 项自动化测试；Lint、类型检查与生产构建；隔离执行器的接口与浏览器全流程联调；桌面和手机布局核对。详见 docs/evidence/工作台/验收记录.md。
- 待验：真实 Codex 模型生成质量与真实编码任务。联调没有调用真实模型，不把测试替身结果计为模型验收。
- 运行：本机 http://127.0.0.1:3100 。

## 决策台账

| 编号 | 决策 | 状态 | 选定结论 | 依据 |
| --- | --- | --- | --- | --- |
| D1 | 信息架构 | 已定 | 一条顺序流程，当前步骤为主，已完成内容归档 | 用户本轮反馈 |
| D2 | 初始产物 | 已定 | PRD 导入后先提问，不平行生成文档或书签 | 原手册 PRD 体检 |
| D3 | 开发授权 | 已定 | 需求确认 → 文档生成/确认 → 书签 → 开发 | 用户要求 |
| D4 | 分步推进 | 已定 | 每阶段可含多个子轮次，每轮独立确认和验收 | 原规则卡阶段表 |
| D5 | 部署形态 | 已定 | 本机单用户，复用 Codex CLI | Git 与本地执行需求 |
| D6 | 真实模型验收 | 待验 | 保留明确待验标记 | 未启动真实模型任务 |

## GitHub 发布

- 完整源码：https://github.com/owenmao-builder/starter-studio
- 公开流程演示：https://owenmao-builder.github.io/starter-studio/
- Pages 仅部署静态前端；完整模型开发与 Git 功能继续由本机服务执行。
- 新增 2 项预览测试，验证逐步确认、浏览器持久化与能力标注；总计 16 项。

## 导航修复

- 「我的项目」由静态标题改为可点击的项目列表入口，保留侧栏最近项目快捷选择。
- 「项目记录」不再因未选择项目而禁用；支持空状态、选择项目和切换项目。
- 页面地址记录 view 与 project，刷新和浏览器返回恢复对应页面；保持 GitHub Pages 子路径。
